import type { Texture } from "pixi.js";

export interface AppTextures {
  card: Texture;
  cardGlow: Texture;
  flame: Texture;
  ember: Texture;
  bubble: Texture;
  bgMenu: Texture;
  bgAce: Texture;
  bgMagic: Texture;
  bgPhoenix: Texture;
  uiButtonIdle: Texture;
  uiButtonHover: Texture;
  uiPanelFrame: Texture;
  particleSpark: Texture;
}
